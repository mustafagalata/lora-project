# LLM Final Project — bağımlılık listesi
#
# NOT: Colab/Kaggle notebook'ları bu dosyayı KULLANMAZ; kendi hücrelerinde
# torch/torchvision'a dokunmadan HW2-style kurulum yapar. Bu liste lokal
# geliştirme veya alternatif ortamlar için referanstır.

# --- Core ML (torch ve torchvision'ı bilinçli pin'lemiyoruz) ---
transformers
peft
trl
accelerate
bitsandbytes
datasets
sentencepiece

# --- RAG ---
langchain
langchain-community
langchain-huggingface
langchain-text-splitters
sentence-transformers
faiss-cpu>=1.8.0

# --- Document processing ---
pymupdf
pypdf
pdfplumber
tiktoken

# --- Evaluation: COMET ailesini --no-deps ile kurmak gerekiyor; o yüzden
#     Colab notebook'ta ayrı bir komutta yapıyoruz. Lokal kurulumda da
#     "pip install --no-deps unbabel-comet" + "lightning torchmetrics" akışını izle.
unbabel-comet
lightning>=2.0
torchmetrics

# --- Task 3 agent design (yalnız tasarım; runtime kullanmıyoruz) ---
langdetect
wikipedia-api

# --- Utilities ---
pyyaml>=6.0
pandas
numpy
tqdm
matplotlib
python-dotenv
